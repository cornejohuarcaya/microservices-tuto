server:
  port: 8083
  
spring:
  application:
    name: CONFIG-SERVICE
  cloud:
    config:
      server:
        git:
          default-label: eureka
          uri: https://github.com/cornejohuarcaya/microservices-tuto.git
          clone-on-start: true
          force-pull: false
          refresh-rate: 15
          
